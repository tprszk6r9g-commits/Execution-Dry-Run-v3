# v3.6B.1 Registry Hardening
Closes nonce reuse and action-digest replay; pins one authorized executor; adds guardian pause-only authority, signed-passport digest binding, stricter validity/risk bounds, expanded audit hashing, a mainnet-blocked deployment script, and dedicated Foundry CI. The registry still never executes trades or calls TBAs/tokens/routers.
